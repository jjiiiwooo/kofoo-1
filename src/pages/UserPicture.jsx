import React from 'react';
import Camera from '../components/Camera';

const UserPicture = () => {

    return (
        <div>
            <Camera />
        </div>
    );
};

export default UserPicture;
